rd_("AbHeight of the box.AaWidth of the box.BmBreadth or depth coordinate, depending on \xe2\x80\xa6CiWhere the normalized drawing starts along <code>x</code>.BmDepth or breadth coordinate, depending on \xe2\x80\xa6CiWhere the normalized drawing starts along <code>y</code>.CcA deterministic PRNG, so that a failing case is \xe2\x80\xa6CjA caller-chosen label, one-based for the flat API (see \xe2\x80\xa6AoHow many nodes the arena holds.oAn empty arena.DeA top down drawing of <code>root</code>, in top-left coordinates, \xe2\x80\xa6BcAn engine using the scalar kernels.BkSeeds the generator; <code>tg_srand</code>.BmThe top edge of a box; see <code>left</code>.CkWhich implementation to dump or check; they all have to \xe2\x80\xa6CnA node of a hierarchy that acts as a tree, the counterpart \xe2\x80\xa6oSmall integers.DjThe differential oracle of <code>test/overlap.c --dump</code>, and its \xe2\x80\xa6CmA struct-of-arrays mirror of the tree, laid out by sweeps \xe2\x80\xa6AoReturns the argument unchanged.000000000000BaCalls <code>U::from(self)</code>.000000000000AmThe nodes, in creation order.DcThe phases as <code>(name, duration)</code>, in the order they run.AgThe left edge of a box.DaA pseudo random value in <code>0..n</code>; <code>tg_rand</code>.B`The root of the tree to lay out.CjCalled once per node, in preorder, during the second walk.CnThe nodes of one or more trees, owning them for as long as \xe2\x80\xa6DhOne tree of the corpus, in the order <code>overlap.c</code> sweeps them.DjWall clock over one large tree, the workload <code>simd-plan.md</code> \xe2\x80\xa6CaBuilding the mirror: allocation, and one copy in.CfA random tree in a fresh arena; <code>tg_build</code>.DcA chain of <code>depth</code> nodes, each the only child of the \xe2\x80\xa6CkCompares the corpus against a dump, value by value; the \xe2\x80\xa6C`The contour walk, which is the algorithm proper.CiThe position of the node in its arena, in creation order.DjThe position of the regime in <code>REGIMES</code>, the value of the C \xe2\x80\xa6DfDistance from the root, set by <code>layout</code>; the root is at \xe2\x80\xa6AeThe depth axis sweep.BfNormalizing the drawing to the origin.AfThe sum of the phases.AlMultiples of ten, no margin.BmA reusable mirror, for laying out repeatedly.CaA handle on a node held by an <code>Arena</code>.CbHow long each phase of a flat layout took; see \xe2\x80\xa6mSize regimes.AnOne implementation under test.BeA wide spread of sizes, with margins.CmCalled once per node as the second walk finalizes it; the \xe2\x80\xa6CdThe far edge of a node along the depth axis, the \xe2\x80\xa6CiThe deepest point met so far; the caller seeds it and \xe2\x80\xa6CaAVX2 where the CPU has it, plain loops otherwise.FeLays out the tree described by <code>input</code>, writing <code>x</code> and <code>y</code> into \xe2\x80\xa6CkLays out the tree, reusing whatever this engine already \xe2\x80\xa6CkExtra separation kept between this node and the sibling \xe2\x80\xa6DgThe parent, set by <code>layout</code>; <code>None</code> for the root.CmWhere the time goes inside one flat layout, which is what \xe2\x80\xa6CkPlain loops, which the compiler is free to vectorize on \xe2\x80\xa6CiThe modifier sums, the coordinates and the child spacing.DgThe arrays one <code>layout_api</code> call needs, and the pristine \xe2\x80\xa6BmThe elementwise sweeps, scalar or vectorized.CmThe regimes in the order the C enumerates them, which the \xe2\x80\xa6AeEvery node identical.DgThe best of <code>reps</code> batches, each of enough iterations to \xe2\x80\xa6DiPosition among the parent\xe2\x80\x99s children, set by <code>layout</code>; 0 \xe2\x80\xa6DfGathers the nodes of the subtree in preorder; <code>tg_collect</code>.CcWhether the vectorized kernels are the ones in use.CcMarks a node inserted only to take up space, as \xe2\x80\xa6BhWhich kernels this engine dispatches to.CmStrict overlap: boxes that merely share an edge are tidy, \xe2\x80\xa6D`<code>Engine::layout</code>, reporting how long each sweep took.CkTest support: a deterministic generator of random trees \xe2\x80\xa6DaAdds a childless node, the counterpart of <code>init_tree</code>.BeThe children, in left to right order.CgThe children of <code>t</code>, in left to right order.BgWhether the arena holds no node at all.CmThe ids of the subtree rooted at <code>t</code>, in preorder.Di<code>layout_api</code>, which takes its tree as arrays and gives the \xe2\x80\xa6CgThe two hooks of <code>treeinput_t</code>, as closures.CmThe smallest edge along each axis, i.e. the corner of the \xe2\x80\xa6BjAn engine using AVX2 where the CPU has it.DbA tree of exactly <code>n</code> nodes in breadth-first order; \xe2\x80\xa6CmA random tree added to an existing arena, labelled from 1 \xe2\x80\xa6EfWhether <code>x</code>/<code>y</code> denote the middle of the box rather than its \xe2\x80\xa6EeWhether <code>x</code>/<code>y</code> should denote the middle of each box rather \xe2\x80\xa6CmLays out a tree given as parallel arrays, the counterpart \xe2\x80\xa6Df<code>layout_api_flat</code>, reusing whatever this engine already \xe2\x80\xa6DjThe deepest point of the subtree rooted at <code>t</code>, stopping at \xe2\x80\xa6BiAppends one child to <code>parent</code>.EhWhether the depth axis is <code>y</code> (top down) rather than <code>x</code> (left \xe2\x80\xa6DdWhether the depth axis is <code>y</code> rather than <code>x</code>.BlCopying the coordinates back into the arena.DdWhat to lay out, and how; the data half of <code>treeinput_t</code>.EeLays out the tree with the flat sweeps, writing <code>x</code> and <code>y</code> \xe2\x80\xa6E`<code>layout</code>, with the two observation hooks of <code>treeinput_t</code>.CiThe smallest coordinate along the breadth axis, which \xe2\x80\xa6DdThe same tree as <code>bench_tree</code>, in the parallel arrays \xe2\x80\xa6FeWrites the coordinates of <code>nodes</code> into <code>xy</code> as <code>[x_0.., y_0..]</code>, \xe2\x80\xa6DeReplaces the children of <code>parent</code>, in left to right order.DfCalled for every contour pair <code>(sr, cl, dist)</code> compared \xe2\x80\xa6CiAn empty arena with room for <code>capacity</code> nodes.DeCalled for every contour pair <code>separate</code> compares; the \xe2\x80\xa6FeWrites <code>[x, y, w, h]</code> per node into <code>array</code>, indexed by <code>idx - 1</code>\xe2\x80\xa6DcThe state threaded through <code>max_bottom_between</code>, the \xe2\x80\xa6Df<code>crate::layout_api</code>, without ever building a tree of nodes.Dj<code>layout_flat</code>, with the elementwise sweeps vectorized where \xe2\x80\xa6DkBuilds the arena of the flat description used by <code>layout_api</code>\xe2\x80\xa6DkThe deepest point of everything that lies between <code>from</code> and \xe2\x80\xa6BnDrawing non-layered tidy trees in linear time.")