rd_("AbHeight of the box.0AaWidth of the box.0BmBreadth or depth coordinate, depending on \xe2\x80\xa6CiWhere the normalized drawing starts along <code>x</code>.10BmDepth or breadth coordinate, depending on \xe2\x80\xa6CiWhere the normalized drawing starts along <code>y</code>.10CcA deterministic PRNG, so that a failing case is \xe2\x80\xa60CjA caller-chosen label, one-based for the flat API (see \xe2\x80\xa60AoHow many nodes the arena holds.0oAn empty arena.DeA top down drawing of <code>root</code>, in top-left coordinates, \xe2\x80\xa6BcAn engine using the scalar kernels.BkSeeds the generator; <code>tg_srand</code>.3210BmThe top edge of a box; see <code>left</code>.0CkWhich implementation to dump or check; they all have to \xe2\x80\xa60CnA node of a hierarchy that acts as a tree, the counterpart \xe2\x80\xa60oSmall integers.0DjThe differential oracle of <code>test/overlap.c --dump</code>, and its \xe2\x80\xa6CmA struct-of-arrays mirror of the tree, laid out by sweeps \xe2\x80\xa60AoReturns the argument unchanged.000000000000000000000000BaCalls <code>U::from(self)</code>.000000000000000000000000AmThe nodes, in creation order.DcThe phases as <code>(name, duration)</code>, in the order they run.10AgThe left edge of a box.0DaA pseudo random value in <code>0..n</code>; <code>tg_rand</code>.0B`The root of the tree to lay out.0CjCalled once per node, in preorder, during the second walk.0CnThe nodes of one or more trees, owning them for as long as \xe2\x80\xa60DhOne tree of the corpus, in the order <code>overlap.c</code> sweeps them.DjWall clock over one large tree, the workload <code>simd-plan.md</code> \xe2\x80\xa6CaBuilding the mirror: allocation, and one copy in.CfA random tree in a fresh arena; <code>tg_build</code>.10DcA chain of <code>depth</code> nodes, each the only child of the \xe2\x80\xa60CkCompares the corpus against a dump, value by value; the \xe2\x80\xa6C`The contour walk, which is the algorithm proper.0CiThe position of the node in its arena, in creation order.DjThe position of the regime in <code>REGIMES</code>, the value of the C \xe2\x80\xa610DfDistance from the root, set by <code>layout</code>; the root is at \xe2\x80\xa60AeThe depth axis sweep.0BfNormalizing the drawing to the origin.0AfThe sum of the phases.0AlMultiples of ten, no margin.0BmA reusable mirror, for laying out repeatedly.0CaA handle on a node held by an <code>Arena</code>.0CbHow long each phase of a flat layout took; see \xe2\x80\xa60mSize regimes.0AnOne implementation under test.BeA wide spread of sizes, with margins.0CmCalled once per node as the second walk finalizes it; the \xe2\x80\xa60CdThe far edge of a node along the depth axis, the \xe2\x80\xa6CiThe deepest point met so far; the caller seeds it and \xe2\x80\xa610CaAVX2 where the CPU has it, plain loops otherwise.0FeLays out the tree described by <code>input</code>, writing <code>x</code> and <code>y</code> into \xe2\x80\xa6CkLays out the tree, reusing whatever this engine already \xe2\x80\xa610CkExtra separation kept between this node and the sibling \xe2\x80\xa60DgThe parent, set by <code>layout</code>; <code>None</code> for the root.0CmWhere the time goes inside one flat layout, which is what \xe2\x80\xa6CkPlain loops, which the compiler is free to vectorize on \xe2\x80\xa60CiThe modifier sums, the coordinates and the child spacing.0DgThe arrays one <code>layout_api</code> call needs, and the pristine \xe2\x80\xa6BmThe elementwise sweeps, scalar or vectorized.0CmThe regimes in the order the C enumerates them, which the \xe2\x80\xa60AeEvery node identical.0DgThe best of <code>reps</code> batches, each of enough iterations to \xe2\x80\xa6DiPosition among the parent\xe2\x80\x99s children, set by <code>layout</code>; 0 \xe2\x80\xa60DfGathers the nodes of the subtree in preorder; <code>tg_collect</code>.0CcWhether the vectorized kernels are the ones in use.0CcMarks a node inserted only to take up space, as \xe2\x80\xa60BhWhich kernels this engine dispatches to.0CmStrict overlap: boxes that merely share an edge are tidy, \xe2\x80\xa60D`<code>Engine::layout</code>, reporting how long each sweep took.0CkTest support: a deterministic generator of random trees \xe2\x80\xa60DaAdds a childless node, the counterpart of <code>init_tree</code>.0BeThe children, in left to right order.CgThe children of <code>t</code>, in left to right order.10BgWhether the arena holds no node at all.0CmThe ids of the subtree rooted at <code>t</code>, in preorder.0Di<code>layout_api</code>, which takes its tree as arrays and gives the \xe2\x80\xa6CgThe two hooks of <code>treeinput_t</code>, as closures.0CmThe smallest edge along each axis, i.e. the corner of the \xe2\x80\xa60BjAn engine using AVX2 where the CPU has it.0DbA tree of exactly <code>n</code> nodes in breadth-first order; \xe2\x80\xa60CmA random tree added to an existing arena, labelled from 1 \xe2\x80\xa60EfWhether <code>x</code>/<code>y</code> denote the middle of the box rather than its \xe2\x80\xa6EeWhether <code>x</code>/<code>y</code> should denote the middle of each box rather \xe2\x80\xa610CmLays out a tree given as parallel arrays, the counterpart \xe2\x80\xa6Df<code>layout_api_flat</code>, reusing whatever this engine already \xe2\x80\xa610DjThe deepest point of the subtree rooted at <code>t</code>, stopping at \xe2\x80\xa60BiAppends one child to <code>parent</code>.0EhWhether the depth axis is <code>y</code> (top down) rather than <code>x</code> (left \xe2\x80\xa6DdWhether the depth axis is <code>y</code> rather than <code>x</code>.10BlCopying the coordinates back into the arena.0DdWhat to lay out, and how; the data half of <code>treeinput_t</code>.0EeLays out the tree with the flat sweeps, writing <code>x</code> and <code>y</code> \xe2\x80\xa60E`<code>layout</code>, with the two observation hooks of <code>treeinput_t</code>.0CiThe smallest coordinate along the breadth axis, which \xe2\x80\xa60DdThe same tree as <code>bench_tree</code>, in the parallel arrays \xe2\x80\xa60FeWrites the coordinates of <code>nodes</code> into <code>xy</code> as <code>[x_0.., y_0..]</code>, \xe2\x80\xa60DeReplaces the children of <code>parent</code>, in left to right order.0DfCalled for every contour pair <code>(sr, cl, dist)</code> compared \xe2\x80\xa60CiAn empty arena with room for <code>capacity</code> nodes.0DeCalled for every contour pair <code>separate</code> compares; the \xe2\x80\xa60FeWrites <code>[x, y, w, h]</code> per node into <code>array</code>, indexed by <code>idx - 1</code>\xe2\x80\xa60DcThe state threaded through <code>max_bottom_between</code>, the \xe2\x80\xa60Df<code>crate::layout_api</code>, without ever building a tree of nodes.0Dj<code>layout_flat</code>, with the elementwise sweeps vectorized where \xe2\x80\xa60DkBuilds the arena of the flat description used by <code>layout_api</code>\xe2\x80\xa60DkThe deepest point of everything that lies between <code>from</code> and \xe2\x80\xa60BnDrawing non-layered tidy trees in linear time.")